faster-whisper
rich
sounddevice
soundfile
numpy
python-dotenv
gdown
openai
flask
waitress
# Speaker diarization (cross-platform). Needs HF_TOKEN in .env — see README.
pyannote.audio
# pyannote deps still import pkg_resources, removed in setuptools>=81
setuptools<81
# Legacy diarization fallback, not installable on Windows. Optional:
# nemo_toolkit[asr]
